import styled from "styled-components";
import { fontSize } from "../../stylesheet";

export const BoldCenterText = styled.div`
  position: absolute;
  top: 50%;
  left: 35%;
  font-size: ${fontSize.mainHeader};
  font-weight: bold;
`;
